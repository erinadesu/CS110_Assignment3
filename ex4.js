const arr = [3, 6, 9];
result = [];
for (let index = arr.length - 1; index >= 0; index--) {
    result.push(arr[index]);
    
}
// for (let index = arr.length - 1; index >= 0; index--) {
//     result += arr[index];
// }
// console.log([result])
// Used Google and found out about push so decided to use it. :)
console.log(result)